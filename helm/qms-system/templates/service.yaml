apiVersion: v1
kind: Service
metadata:
  name: {{ include "qms-system.fullname" . }}
  labels:
    {{- include "qms-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qms-system.selectorLabels" . | nindent 4 }}